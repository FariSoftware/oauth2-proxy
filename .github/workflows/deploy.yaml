on:
  push:
    branches:
      - 'master'

name: Deploy to Azure

jobs:
    build-and-deploy:
        runs-on: ubuntu-latest
        steps:
        # checkout the repo
        - name: 'Checkout GitHub Action'
          uses: actions/checkout@main
          
        - name: Log in to Azure
          uses: azure/login@v1
          with:
            creds: ${{ secrets.AZURE_CREDENTIALS }}

        - name: Build and deploy Container App
          uses: azure/container-apps-deploy-action@v1
          with:
            appSourcePath: ${{ github.workspace }}
            registryUrl: ghcr.io
            containerAppName: oauth2-proxy
            resourceGroup: finkonu-dev
            registryUsername: matspi
            registryPassword: ${{ secrets.GH_TOKEN }}
            imageToBuild: ghcr.io/FariSoftware/oauth2-proxy
            containerAppEnvironment: finkonu-dev-container-environment
            targetPort: 4180
